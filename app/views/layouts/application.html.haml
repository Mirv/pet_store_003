<!DOCTYPE html>
.html
.head
  .title
    %h1 Menu
    = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true 
    = javascript_include_tag 'application', 'data-turbolinks-track' => true 
    = csrf_meta_tags

.body
  = render 'layouts/menu'
  // TODO - this needs to be fixed so no crashy the server
  // = render 'layouts/flash_messages' if flash.present?  
  = yield 
